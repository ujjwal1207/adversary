/**
 * @adversary/rails
 *
 * The Rail interface and its two implementations. Nothing in packages/agents
 * may import this package (docs/ARCHITECTURE.md 5.2).
 *
 * Phase 4  - the Rail interface and the deterministic mock rail
 * Phase 10 - the live-test rail, which refuses to construct on a production key
 */

export {};
