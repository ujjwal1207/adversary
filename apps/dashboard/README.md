# apps/dashboard

Phase 11. React + Vite + Tailwind, no component library. Three screens: run list,
scorecard, trajectory viewer.

Deliberately not scaffolded yet - an empty React app in the workspace would slow
every install and build for ten phases before it renders anything. See
`docs/ARCHITECTURE.md` 13.3 for what it must do, including the visible SYNTHETIC
badge on any scenario using synthetic webhooks.
