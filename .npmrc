# Non-hoisted node_modules is not a preference here — it is the enforcement
# mechanism for the dependency rule in docs/ARCHITECTURE.md 5.2: packages/agents
# does not depend on @adversary/rails, so under this layout the import does not
# resolve at all. Do not enable hoisting.
node-linker=isolated
shamefully-hoist=false
strict-peer-dependencies=false
